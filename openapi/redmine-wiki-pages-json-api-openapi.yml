openapi: 3.1.0
info:
  title: Redmine REST Attachments Wiki Pages.json API
  description: REST API for Redmine project management. Base URL is your Redmine instance URL.
  version: 1.0.0
servers:
- url: https://{redmine_host}
  description: Redmine instance
  variables:
    redmine_host:
      default: redmine.example.com
security:
- apiKeyHeader: []
- apiKeyQuery: []
- basicAuth: []
tags:
- name: Wiki Pages.json
paths:
  /wiki_pages.json:
    get:
      summary: List wiki pages
      responses:
        '200':
          description: Wiki pages
      tags:
      - Wiki Pages.json
    post:
      summary: Create wiki page
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '201':
          description: Created
      tags:
      - Wiki Pages.json
components:
  securitySchemes:
    apiKeyHeader:
      type: apiKey
      in: header
      name: X-Redmine-API-Key
    apiKeyQuery:
      type: apiKey
      in: query
      name: key
    basicAuth:
      type: http
      scheme: basic
